# Development Backlog

## Sequence

```text
Foundation
→ Database
→ Auth/RBAC
→ Product
→ Inventory
→ POS
→ Payment/Cash
→ Receipt
→ Purchase/Supplier
→ Expense
→ Customer
→ Return/Void
→ IndexedDB
→ Offline POS
→ Sync
→ Dashboard
→ Reports
→ Fuel
→ Settings/Audit
→ PWA/Hardware
→ Testing
→ Pilot
→ Production
```

## P0
Auth, product, inventory, POS, payment, cash, offline, sync.

## P1
Purchase, supplier, expense, return, void, dashboard, reports.

## P2
Customer, fuel analytics, advanced device/printer features.

## P3
Multi-store, loyalty, accounting, payment gateway, fuel sensor, customer display.

## Definition of Done
UI + API + validation + permissions + error handling + offline behavior where relevant + tests + lint + type check.
